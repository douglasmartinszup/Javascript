//Combinando Arrays 


const numeros = [1,2,3,4,5];


const combinado = numeros.join('/');
console.log(combinado);

//Ou inserindo espaços

const frase = 'Seja bem vindo ao curso';

const espaco = frase.split(' ');
console.log(espaco);